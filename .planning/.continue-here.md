---
context: new-project-setup
phase: null
task: 5
total_tasks: 10
status: in_progress
workflow: gsd-new-project
workflow_step: requirements-definition
last_updated: 2026-04-22T22:32:51.368Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Constraints discovered this session.
> Acknowledge each one before proceeding.

- [ ] CONSTRAINT: Priority mapping inverted in brief — Todoist `priority: 4` = p1 (urgent/highest), `priority: 1` = no priority. The brief says "Highest → 1" which is WRONG. Correct: Highest→4, High→3, Normal→2, Low/unset→1. Reverse: Todoist 1→Zoho unset (NOT "Low"). Must be unit-tested before any sync code runs.
- [ ] CONSTRAINT: Description sync is OUT of v1 scope. User explicitly excluded it. Todoist description field is used ONLY for the `[zoho:ID]` footer metadata. Do not add description to the canonical hash or field mapping.
- [ ] CONSTRAINT: Migration is NOT a fresh seed. User already has all tasks synced in Todoist via Make.com with user-applied labels. Migration must LINK existing Todoist tasks to Zoho tasks (preserving labels), not create duplicates. The Make.com scenario stores a unique identifier — see blocker below.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Inverted priority mapping | Brief maps Highest→1; Todoist 1=no priority. Silently wrong-but-plausible. | blocking | Unit test round-trip: Zoho Highest→Todoist 4→Zoho Highest |
| Fresh seed over existing data | Running a create-all seed will duplicate every task already in Todoist. | blocking | Migration script must check for existing Todoist tasks before creating |

<current_state>
Mid-`/gsd-new-project` at Step 7 (requirements definition). All prior steps complete and committed:
- PROJECT.md ✓ (commit 8e6c987)
- config.json ✓ (commit 5cf59ee)
- Research (STACK, FEATURES, ARCHITECTURE, PITFALLS, SUMMARY) ✓ (commit 982d297)

Requirements scoping is ~80% done. All feature categories answered EXCEPT the migration seed spec, which is blocked on a Make.com API key inspection.

**Blocked on:** User was about to share their Make.com API key so the agent can inspect the scenario and find how Zoho task ID is stored in Todoist tasks. Resume by asking user to paste the Make.com API key.
</current_state>

<completed_work>

- Task 1: Deep questioning — Done
- Task 2: PROJECT.md written and committed (8e6c987)
- Task 3: Workflow preferences collected + config.json committed (5cf59ee)
- Task 4: Domain research — 4 parallel researchers (STACK, FEATURES, ARCHITECTURE, PITFALLS) + synthesizer (SUMMARY) — committed (982d297)
- Task 5: Requirements scoping (partial):
  - Infrastructure category: ALL in v1 ✓
  - Core Sync: canonical hash, Zoho→Todoist create, bidirectional field sync, completion sync ✓
  - Loop Safety: echo suppression, label-only skip, 2s defer on Zoho jobs ✓ (note: user skipped arq SETNX lock — may want to revisit)
  - Edge Cases: reassignment-away delete+email, Todoist delete→Zoho delete+email, Todoist creation no-op, missing footer=orphan ✓
  - Seed/Recon: one-time seed, reconciliation cron, orphan detection ✓ (but seed spec changed — see blockers)
  - Observability: /health, daily summary task, sync_events log, 90-day cleanup ✓
</completed_work>

<remaining_work>

- Task 5 (cont): Finalise migration seed spec once Make.com API key inspected
- Task 6: Human action — user shares Make.com API key → inspect scenario → determine ID storage format
- Task 7: Write .planning/REQUIREMENTS.md with REQ-IDs
- Task 8: Spawn gsd-roadmapper → ROADMAP.md + STATE.md
- Task 9: Present roadmap for approval
- Task 10: Generate CLAUDE.md + final commit
</remaining_work>

<decisions_made>

- Python 3.12 + FastAPI + arq + Postgres + Redis, two Railway services (web + worker) for crash isolation
- Canonical hash loop prevention; footer `[zoho:ID]` in Todoist description (not labels)
- **Priority mapping CORRECTED**: Highest→4, High→3, Normal→2, Low/unset→1 (brief had it inverted)
- Reassignment-away: delete Todoist task + Resend email
- Todoist delete → delete in Zoho + Resend email (user accepted the deletion risk)
- No description sync in v1; Todoist description used only for `[zoho:ID]` footer
- Migration mode (not fresh seed): link existing Make.com-synced Todoist tasks
- E2E test with dummy tasks before running live migration
- Resend already configured via YNAB on same Railway account (resend==6.9.4)
- YOLO mode, Standard granularity, research+plan_check+verifier all enabled, balanced model
- V2 feature noted (not in scope): include Zoho task link + associated Deal title in Todoist content
</decisions_made>

<blockers>

- **Make.com API key pending**: User was about to share API key to inspect how Make.com stores Zoho task ID in Todoist tasks. This determines the migration match strategy. Without this, migration spec can't be finalised (could fall back to title-match as a safe default, but ID-based match is more reliable).
</blockers>

## Required Reading (in order)
1. `.planning/PROJECT.md` — full project context, all design decisions
2. `.planning/research/SUMMARY.md` — synthesised research findings, critical corrections to the brief
3. `.planning/research/FEATURES.md` — Todoist priority numbering correction (most critical)
4. `.planning/research/ARCHITECTURE.md` — arq dedup pattern, SELECT FOR UPDATE, two-cycle orphan detection
5. `.planning/research/PITFALLS.md` — Zoho stale-read race, hash normalisation gaps, Make.com failure analysis

## Infrastructure State
- Railway: service not yet created (Phase 1 work)
- Make.com scenario: still active (user has not disabled it yet)
- Zoho custom field `Todoist_Task_ID`: not yet created (Phase 1 work)
- Todoist project for sync: exists (was created for Make.com sync)
- Resend: account exists (via YNAB), API key needs adding as Railway env var

## Open Questions for Phase 1 (from research)
1. Exact `api_name` of `Todoist_Task_ID` custom field after creation
2. Actual Zoho Status picklist values (any custom done-statuses?)
3. Raw `Due_Date` format from this org (date-only or datetime with timezone?)
4. Zoho webhook payload for deletion vs reassignment events
5. Todoist Sync API project filtering (server-side or client-side?)
6. arq cron `"*/15 * * * *"` syntax support in pinned version
7. Zoho org region for API base URL

<context>
We are at the exact midpoint of gsd-new-project. Everything up to requirements is done. The only remaining input needed is the Make.com scenario inspection (to finalise migration strategy). Once that's done, REQUIREMENTS.md can be written in one pass and the roadmapper spawned immediately — 30 minutes of work left in this workflow.

The brief was excellent and research confirmed most of it. Key changes from research: priority mapping inverted, description sync removed from scope, migration is link-not-create, Zoho webhook has no retry (reconciliation is primary), Todoist Sync API is not time-based (must run consistently).

The roadmap will closely follow the 8-phase structure in the brief, with modifications for: (a) migration phase instead of fresh seed, (b) E2E test phase before migration, (c) arq SETNX lock recommendation.
</context>

<next_action>
Ask user: "Ready to share the Make.com API key? Paste it and I'll inspect the scenario to find the Zoho→Todoist ID linkage format, then we can finish requirements and create the roadmap."

If user prefers to skip Make.com inspection, fall back to: title-based match for migration (flag low-confidence matches for manual review).
</next_action>
