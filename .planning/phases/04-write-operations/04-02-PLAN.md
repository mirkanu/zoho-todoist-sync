---
phase: 04-write-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/zoho/writer.py
  - tests/unit/test_zoho_writer.py
autonomous: true
requirements: [SYNC-3, SYNC-6, EDGE-2, EDGE-3, EDGE-4, EDGE-6]

must_haves:
  truths:
    - "update_zoho_task issues PUT /crm/v8/Tasks/{id} with body {data:[{Subject, Due_Date, Priority}]}; Priority is the reverse-mapped Zoho string (4->Highest, 3->High, 2->Normal, 1->Low)"
    - "When normalised.due_date is None, the PUT body contains Due_Date: null (Python None -> JSON null) - field is cleared, not omitted"
    - "complete_zoho_task writes Status = settings.zoho_terminal_statuses_list[0] (NOT a hardcoded 'Completed' string)"
    - "delete_zoho_task calls DELETE /crm/v8/Tasks/{id}; a 404 response is idempotent (no exception, no email sent); on successful delete, a Resend email is sent to manuelkuhs@gmail.com"
    - "Resend failure inside delete_zoho_task is caught and logged - does NOT re-raise (EDGE-6)"
    - "write_todoist_id_to_zoho raises ZohoAPIError if zoho_field_cache['todoist_task_id_api_name'] is missing/None (Pitfall 3)"
    - "write_todoist_id_to_zoho writes {field_api_name: todoist_task_id} using the cached api_name - not hardcoded"
    - "Zoho 207 Multi-Status response with per-record status != 'success' raises ZohoAPIError"
    - "All write functions raise ZohoAuthError/ZohoNotFoundError/ZohoRateLimitError/ZohoAPIError correctly for 401/404/429/other"
  artifacts:
    - path: "app/zoho/writer.py"
      provides: "update_zoho_task, complete_zoho_task, delete_zoho_task, write_todoist_id_to_zoho + _zoho_handle + _send_deletion_notification"
      min_lines: 130
    - path: "tests/unit/test_zoho_writer.py"
      provides: "Unit tests covering SYNC-3/6, EDGE-2/3/4/6 + 207 + auth/404/429"
      min_lines: 170
  key_links:
    - from: "app/zoho/writer.py"
      to: "Zoho v8 REST API"
      via: "httpx.AsyncClient PUT/DELETE"
      pattern: "client\\.(put|delete)\\(.*Tasks"
    - from: "app/zoho/writer.py"
      to: "app.zoho.state.zoho_field_cache"
      via: "write_todoist_id_to_zoho reads todoist_task_id_api_name"
      pattern: "zoho_field_cache"
    - from: "app/zoho/writer.py"
      to: "app.core.priority.todoist_to_zoho_priority"
      via: "Priority reverse mapping for update_zoho_task"
      pattern: "todoist_to_zoho_priority"
    - from: "app/zoho/writer.py"
      to: "settings.zoho_terminal_statuses_list"
      via: "complete_zoho_task uses first terminal status"
      pattern: "zoho_terminal_statuses_list"
    - from: "app/zoho/writer.py"
      to: "resend.Emails.send_async"
      via: "Deletion notification"
      pattern: "resend\\.Emails\\.send_async"
---

<objective>
Build the Zoho writer module (`app/zoho/writer.py`) implementing update / complete / delete / write-back-Todoist-ID
against Zoho CRM v8 REST (raw `httpx.AsyncClient`, same style as `app/zoho/client.py`), plus Resend-backed
deletion notifications. Cover every behaviour with unit tests using the `httpx_mock` fixture from `pytest-httpx`.

Purpose: Phase 4 goal two - the Todoist->Zoho write path plus the SYNC-6 ID linkage write-back that
`create_todoist_task` (Plan 01) relies on downstream. After this plan, the sync_task worker (Phase 5) can
mutate Zoho records, complete them using configured terminal statuses, delete them on Todoist-side
deletion, and persist the Todoist ID into the Zoho custom field.

Output:
- `app/zoho/writer.py` - four async functions + helpers
- `tests/unit/test_zoho_writer.py` - 15 pytest-asyncio tests
</objective>

<execution_context>
@/data/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-write-operations/04-RESEARCH.md
@.planning/phases/04-write-operations/04-PATTERNS.md
@.planning/phases/04-write-operations/04-VALIDATION.md
@app/zoho/client.py
@app/zoho/state.py
@app/core/priority.py
@app/core/config.py
@app/core/normalise.py
@tests/unit/test_zoho_client.py
@tests/conftest.py

<interfaces>
From app/core/normalise.py:

  @dataclass(frozen=True)
  class NormalisedTask:
      title: str
      due_date: str | None  # "YYYY-MM-DD" or None
      priority: int         # Todoist int 1..4
      is_completed: bool

From app/zoho/client.py (import - do NOT redefine):

  ZOHO_EU_BASE_URL = "https://www.zohoapis.eu/crm/v8"
  class ZohoAuthError(Exception): ...       # 401
  class ZohoNotFoundError(Exception): ...   # 404
  class ZohoRateLimitError(Exception): ...  # 429
  class ZohoAPIError(Exception): ...        # other non-2xx

From app/zoho/state.py (module-level mutable dict):

  zoho_field_cache: ZohoFieldCache = {}
  # Populated at startup by main.lifespan:
  #   zoho_field_cache["todoist_task_id_api_name"]  -> str | None

From app/core/priority.py:

  def todoist_to_zoho_priority(todoist_priority: int) -> str
  # 4->"Highest", 3->"High", 2->"Normal", 1->"Low"; fallback "Low"

From app/core/config.py:

  class Settings:
      @property
      def zoho_terminal_statuses_list(self) -> list[str]

Zoho v8 REST payload shapes (RESEARCH Pattern 3 + 5):

  # Update (PUT /Tasks/{id}):
  {"data": [{"Subject": str, "Due_Date": "YYYY-MM-DD" | None, "Priority": "Highest"|"High"|"Normal"|"Low"}]}
  # Success: 200/201 JSON {"data": [{"status": "success", ...}]}
  # Partial: 207 JSON {"data": [{"status": "success"|"error", ...}]} - check per-record

  # Complete (PUT /Tasks/{id}):
  {"data": [{"Status": "<first terminal status>"}]}

  # Delete (DELETE /Tasks/{id}):   no body; 2xx success; 404 = already gone (idempotent)

  # Write Todoist ID (PUT /Tasks/{id}):
  {"data": [{<zoho_field_cache["todoist_task_id_api_name"]>: "<todoist_task_id>"}]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for Zoho writer</name>
  <files>tests/unit/test_zoho_writer.py</files>
  <read_first>
    - tests/unit/test_zoho_client.py (analog - httpx_mock patterns, base URL constant, header assertions)
    - tests/conftest.py (complete_env fixture; pytest-httpx provides httpx_mock)
    - app/zoho/client.py (typed exceptions + ZOHO_EU_BASE_URL)
    - app/zoho/state.py (zoho_field_cache - must be cleared/restored per test)
    - app/core/priority.py (todoist_to_zoho_priority - expected outputs)
    - app/core/normalise.py (NormalisedTask)
    - .planning/phases/04-write-operations/04-PATTERNS.md (Zoho test patterns)
    - .planning/phases/04-write-operations/04-RESEARCH.md (Pattern 3, Pattern 5, Pitfall 2, Pitfall 3, Pitfall 5)
  </read_first>
  <behavior>
    Tests MUST fail at the end of this task with ImportError / ModuleNotFoundError on
    `from app.zoho.writer import ...`. The 15 required test functions (each marked
    @pytest.mark.asyncio, each using the `complete_env` fixture so get_settings() works):

    1. test_update_zoho_task_correct_payload(httpx_mock, complete_env) - SYNC-3
       Stub: httpx_mock.add_response(url=f"{ZOHO_BASE}/Tasks/Z1", method="PUT",
             status_code=200, json={"data":[{"status":"success","code":"SUCCESS","details":{},"message":"record updated"}]})
       Input: NormalisedTask(title="New title", due_date="2026-06-01", priority=3, is_completed=False)
       Call: await update_zoho_task("Z1", normalised, access_token="tok")
       req = httpx_mock.get_request(); body = json.loads(req.content)
       Assert body == {"data": [{"Subject": "New title", "Due_Date": "2026-06-01", "Priority": "High"}]}
       Assert req.headers["Authorization"] == "Zoho-oauthtoken tok"

    2. test_update_zoho_task_clears_due_date_with_null(httpx_mock, complete_env) - EDGE-3 / A1
       200 response stub; NormalisedTask(title="T", due_date=None, priority=1, is_completed=False)
       Assert body["data"][0]["Due_Date"] is None
       Assert "Due_Date" in body["data"][0]   # key MUST be present, value null; NOT omitted
       Assert body["data"][0]["Priority"] == "Low"

    3. test_update_zoho_task_priority_roundtrip(httpx_mock, complete_env) - SYNC-2 reverse
       Use @pytest.mark.parametrize("todoist_int,zoho_str", [(4,"Highest"),(3,"High"),(2,"Normal"),(1,"Low")])
       For each case, stub 200, send a PUT, assert body["data"][0]["Priority"] == zoho_str

    4. test_update_zoho_task_401_raises_auth_error(httpx_mock, complete_env)
       httpx_mock.add_response(status_code=401, json={}); expect ZohoAuthError

    5. test_update_zoho_task_404_raises_not_found(httpx_mock, complete_env)
       status_code=404; expect ZohoNotFoundError

    6. test_update_zoho_task_429_raises_rate_limit(httpx_mock, complete_env)
       status_code=429; expect ZohoRateLimitError

    7. test_update_zoho_task_207_with_error_raises(httpx_mock, complete_env) - Pitfall 2
       status_code=207, json={"data":[{"status":"error","code":"INVALID_DATA","message":"bad"}]}
       expect pytest.raises(ZohoAPIError, match="207")

    8. test_update_zoho_task_207_success_passes(httpx_mock, complete_env) - Pitfall 2
       status_code=207 with {"status":"success", ...}; call must NOT raise

    9. test_complete_zoho_task_uses_terminal_status(httpx_mock, complete_env, monkeypatch) - EDGE-4
       monkeypatch.setenv("ZOHO_TERMINAL_STATUSES", "Done,Cancelled")
       from app.core.config import get_settings; get_settings.cache_clear()
       stub PUT /Tasks/Z3 200; await complete_zoho_task("Z3", access_token="tok")
       Parse body; assert body["data"][0]["Status"] == "Done"  # first entry, NOT "Completed"
       Teardown: get_settings.cache_clear()

    10. test_complete_zoho_task_default_completed(httpx_mock, complete_env) - EDGE-4 default
        complete_env default ZOHO_TERMINAL_STATUSES="Completed" (from Settings default)
        Assert body["data"][0]["Status"] == "Completed"

    11. test_delete_zoho_task_sends_email(httpx_mock, complete_env, monkeypatch) - EDGE-2
        httpx_mock.add_response(url=f"{ZOHO_BASE}/Tasks/Z1", method="DELETE", status_code=200,
            json={"data":[{"status":"success","code":"SUCCESS","details":{},"message":"record deleted"}]})
        sent = []
        async def fake(p): sent.append(p)
        monkeypatch.setattr("resend.Emails.send_async", fake)
        monkeypatch.setattr(resend, "api_key", "re_test_key")
        await delete_zoho_task("Z1", access_token="tok")
        Assert len(sent) == 1
        Assert sent[0]["to"] == ["manuelkuhs@gmail.com"]
        Assert "Z1" in sent[0]["subject"] or "Z1" in sent[0]["html"]

    12. test_delete_zoho_task_404_idempotent_no_email(httpx_mock, complete_env, monkeypatch) - Pitfall 5
        httpx_mock.add_response(method="DELETE", status_code=404, json={})
        sent capture as above
        await delete_zoho_task("Z1", access_token="tok")  # must NOT raise
        Assert len(sent) == 0

    13. test_delete_zoho_task_resend_failure_does_not_raise(httpx_mock, complete_env, monkeypatch) - EDGE-6
        200 delete stub
        async def boom(p): raise RuntimeError("resend down")
        monkeypatch.setattr("resend.Emails.send_async", boom)
        await delete_zoho_task("Z1", access_token="tok")  # must NOT raise

    14. test_write_todoist_id_to_zoho_uses_cached_field_name(httpx_mock, complete_env) - SYNC-6 + Pitfall 3
        Setup: import app.zoho.state as state; state.zoho_field_cache["todoist_task_id_api_name"] = "Todoist_Task_ID"
        httpx_mock.add_response(url=f"{ZOHO_BASE}/Tasks/Z1", method="PUT", status_code=200,
            json={"data":[{"status":"success","code":"SUCCESS","details":{},"message":"ok"}]})
        await write_todoist_id_to_zoho("Z1", "T999", access_token="tok")
        Parse body; assert body["data"][0] == {"Todoist_Task_ID": "T999"}
        Teardown: state.zoho_field_cache.clear()

    15. test_write_todoist_id_to_zoho_raises_when_field_not_resolved(complete_env) - Pitfall 3
        import app.zoho.state as state; state.zoho_field_cache.clear()
        with pytest.raises(ZohoAPIError, match="not resolved"):
            await write_todoist_id_to_zoho("Z1", "T1", access_token="tok")
        No httpx call expected; does NOT need httpx_mock fixture.

    Test file header (exact imports):

      import json
      import pytest
      import resend
      from app.zoho.writer import (
          update_zoho_task, complete_zoho_task,
          delete_zoho_task, write_todoist_id_to_zoho,
      )
      from app.zoho.client import (
          ZohoAuthError, ZohoNotFoundError, ZohoRateLimitError, ZohoAPIError,
      )
      from app.core.normalise import NormalisedTask

      ZOHO_BASE = "https://www.zohoapis.eu/crm/v8"

    For any test that mutates zoho_field_cache, clear it in teardown to prevent state bleed.
  </behavior>
  <action>
    Create tests/unit/test_zoho_writer.py with the 15 tests specified in <behavior>. For httpx_mock PUT
    responses use a realistic Zoho response body:
    {"data": [{"status": "success", "code": "SUCCESS", "details": {}, "message": "record updated"}]}

    For the Authorization header, assert the exact string "Zoho-oauthtoken tok" (matching Phase 2
    pattern). Before running, note: `pytest-httpx` fixture `httpx_mock` is already installed
    (confirmed by tests/unit/test_zoho_client.py using it).

    Run the test file and confirm every test fails with ModuleNotFoundError / ImportError on the
    `from app.zoho.writer import ...` line. If any test fails with a different error (e.g., fixture
    missing, syntax error), fix the test before committing.

    Commit: `test(04-02): add failing tests for zoho writer`
  </action>
  <verify>
    <automated>cd /data/home/zoho-todoist-sync &amp;&amp; python -m pytest tests/unit/test_zoho_writer.py -x -q 2>&amp;1 | tee /tmp/phase4_02_red.log ; grep -E "ModuleNotFoundError.*app\.zoho\.writer|ImportError.*app\.zoho\.writer" /tmp/phase4_02_red.log</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_zoho_writer.py exists
    - `grep -c "async def test_" tests/unit/test_zoho_writer.py` returns >= 12 (15 tests, parametrise reduces count)
    - Test names present (grep -F): `test_update_zoho_task_correct_payload`, `test_update_zoho_task_clears_due_date_with_null`, `test_update_zoho_task_priority_roundtrip`, `test_update_zoho_task_401_raises_auth_error`, `test_update_zoho_task_404_raises_not_found`, `test_update_zoho_task_429_raises_rate_limit`, `test_update_zoho_task_207_with_error_raises`, `test_update_zoho_task_207_success_passes`, `test_complete_zoho_task_uses_terminal_status`, `test_complete_zoho_task_default_completed`, `test_delete_zoho_task_sends_email`, `test_delete_zoho_task_404_idempotent_no_email`, `test_delete_zoho_task_resend_failure_does_not_raise`, `test_write_todoist_id_to_zoho_uses_cached_field_name`, `test_write_todoist_id_to_zoho_raises_when_field_not_resolved`
    - `grep -F 'from app.zoho.writer import' tests/unit/test_zoho_writer.py` returns 1+
    - `grep -F 'manuelkuhs@gmail.com' tests/unit/test_zoho_writer.py` returns 1+
    - `grep -F 'Zoho-oauthtoken tok' tests/unit/test_zoho_writer.py` returns 1+
    - `grep -F '"Due_Date"' tests/unit/test_zoho_writer.py` returns 2+ (at least the null-clearing test)
    - `grep -F 'zoho_field_cache' tests/unit/test_zoho_writer.py` returns 2+
    - `grep -F 'parametrize' tests/unit/test_zoho_writer.py` returns 1+ (priority roundtrip)
    - pytest run exits non-zero (RED)
    - /tmp/phase4_02_red.log contains line matching ModuleNotFoundError or ImportError for app.zoho.writer
    - git log -1 --oneline contains "test(04-02)"
  </acceptance_criteria>
  <done>All 15 tests written; pytest fails with import errors (RED); commit made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/zoho/writer.py to make tests green</name>
  <files>app/zoho/writer.py, tests/unit/test_zoho_writer.py</files>
  <read_first>
    - app/zoho/client.py (analog - httpx.AsyncClient pattern, _handle dispatch, ZOHO_EU_BASE_URL import source)
    - app/zoho/state.py (zoho_field_cache read access)
    - app/core/priority.py (todoist_to_zoho_priority usage)
    - app/core/config.py (get_settings + zoho_terminal_statuses_list)
    - app/core/normalise.py (NormalisedTask)
    - tests/unit/test_zoho_writer.py (the failing tests from Task 1 - authoritative contract)
    - .planning/phases/04-write-operations/04-RESEARCH.md (Pattern 3 - PUT/DELETE; Pattern 5 - 207 handling; Pattern 4 - Resend)
    - .planning/phases/04-write-operations/04-PATTERNS.md (_zoho_handle skeleton; write_todoist_id_to_zoho skeleton; delete idempotent skeleton)
  </read_first>
  <action>
    Create `app/zoho/writer.py` with this exact structure:

      """Zoho CRM v8 write operations: update / complete / delete / write-back Todoist ID.

      Mirrors app.zoho.client conventions:
      - Raw httpx.AsyncClient per call (matches ZohoClient pattern; avoids stale-token caching)
      - Typed exceptions imported from app.zoho.client (NOT redefined here)
      - _zoho_handle maps HTTP status to typed exception; 207 dispatched to per-record status check
      - Resend email notifications on delete (fire-and-forget, EDGE-6)
      """
      from __future__ import annotations

      from typing import Any

      import httpx
      import resend

      from app.core.config import get_settings
      from app.core.logging import get_logger
      from app.core.normalise import NormalisedTask
      from app.core.priority import todoist_to_zoho_priority
      from app.zoho.client import (
          ZOHO_EU_BASE_URL,
          ZohoAPIError,
          ZohoAuthError,
          ZohoNotFoundError,
          ZohoRateLimitError,
      )
      from app.zoho.state import zoho_field_cache

      log = get_logger(__name__)


      def _zoho_handle(resp: httpx.Response, context: str = "") -> Any:
          """Dispatch Zoho non-2xx + 207 to typed exceptions. Returns parsed JSON on success."""
          if resp.status_code == 401:
              raise ZohoAuthError(f"401 Unauthorized - {context}")
          if resp.status_code == 404:
              raise ZohoNotFoundError(f"404 Not Found - {context}")
          if resp.status_code == 429:
              raise ZohoRateLimitError(f"429 Rate limit - {context}")
          if resp.status_code == 207:
              body = resp.json()
              record = (body.get("data") or [{}])[0]
              if record.get("status") != "success":
                  raise ZohoAPIError(f"207 partial failure - {context}: {record}")
              return body
          if not (200 <= resp.status_code < 300):
              raise ZohoAPIError(f"{resp.status_code} - {context}: {resp.text[:200]}")
          # 204 No Content has no JSON body - don't force-parse
          if resp.status_code == 204 or not resp.content:
              return None
          return resp.json()


      def _auth_headers(access_token: str) -> dict[str, str]:
          return {"Authorization": f"Zoho-oauthtoken {access_token}"}


      async def update_zoho_task(
          zoho_task_id: str,
          normalised: NormalisedTask,
          access_token: str,
      ) -> None:
          """SYNC-3 + EDGE-3: PUT synced fields. Due_Date=None serialises to JSON null (A1)."""
          payload = {
              "Subject": normalised.title,
              "Due_Date": normalised.due_date,   # "YYYY-MM-DD" or None -> JSON null
              "Priority": todoist_to_zoho_priority(normalised.priority),
          }
          async with httpx.AsyncClient() as client:
              resp = await client.put(
                  f"{ZOHO_EU_BASE_URL}/Tasks/{zoho_task_id}",
                  headers=_auth_headers(access_token),
                  json={"data": [payload]},
              )
          _zoho_handle(resp, f"PUT /Tasks/{zoho_task_id}")
          log.info("zoho_task_updated", zoho_id=zoho_task_id)


      async def complete_zoho_task(zoho_task_id: str, access_token: str) -> None:
          """EDGE-4: PUT Status = first configured terminal status. Never hardcode 'Completed'."""
          terminal = get_settings().zoho_terminal_statuses_list[0]
          async with httpx.AsyncClient() as client:
              resp = await client.put(
                  f"{ZOHO_EU_BASE_URL}/Tasks/{zoho_task_id}",
                  headers=_auth_headers(access_token),
                  json={"data": [{"Status": terminal}]},
              )
          _zoho_handle(resp, f"PUT /Tasks/{zoho_task_id} complete")
          log.info("zoho_task_completed", zoho_id=zoho_task_id, status=terminal)


      async def delete_zoho_task(zoho_task_id: str, access_token: str) -> None:
          """EDGE-2: DELETE Zoho task + Resend email. 404 is idempotent (no email). EDGE-6: Resend failure logged."""
          async with httpx.AsyncClient() as client:
              resp = await client.delete(
                  f"{ZOHO_EU_BASE_URL}/Tasks/{zoho_task_id}",
                  headers=_auth_headers(access_token),
              )
          if resp.status_code == 404:
              log.info("zoho_delete_idempotent", zoho_id=zoho_task_id)
              return  # already gone - do NOT send email (Pitfall 5)
          _zoho_handle(resp, f"DELETE /Tasks/{zoho_task_id}")
          log.info("zoho_task_deleted", zoho_id=zoho_task_id)
          await _send_deletion_notification(
              subject=f"[zoho-todoist-sync] Zoho task deleted: {zoho_task_id}",
              html=f"<p>Zoho task <code>{zoho_task_id}</code> was deleted by the sync service "
                   f"(propagation from a Todoist deletion).</p>",
          )


      async def write_todoist_id_to_zoho(
          zoho_task_id: str,
          todoist_task_id: str,
          access_token: str,
      ) -> None:
          """SYNC-6: write Todoist ID into the configured Zoho custom field. Pitfall 3 guards unresolved cache."""
          field_api_name = zoho_field_cache.get("todoist_task_id_api_name")
          if not field_api_name:
              raise ZohoAPIError("todoist_task_id_api_name not resolved - cannot write ID linkage")
          async with httpx.AsyncClient() as client:
              resp = await client.put(
                  f"{ZOHO_EU_BASE_URL}/Tasks/{zoho_task_id}",
                  headers=_auth_headers(access_token),
                  json={"data": [{field_api_name: todoist_task_id}]},
              )
          _zoho_handle(resp, f"PUT /Tasks/{zoho_task_id} write_todoist_id")
          log.info("zoho_todoist_id_written", zoho_id=zoho_task_id, todoist_id=todoist_task_id)


      async def _send_deletion_notification(subject: str, html: str) -> None:
          """Fire-and-forget Resend email. EDGE-6: failure logged, NOT re-raised."""
          try:
              params: resend.Emails.SendParams = {
                  "from": "sync-alerts@resend.dev",  # A3: update to verified domain in Phase 8
                  "to": ["manuelkuhs@gmail.com"],
                  "subject": subject,
                  "html": html,
              }
              await resend.Emails.send_async(params)
          except Exception as exc:
              log.error("resend_email_failed", error=str(exc))
              # Do NOT re-raise - EDGE-6.

    Run the tests - all 15 MUST pass. Then run the full suite to confirm no regression.

    If a test fails, read the diff between expected and actual; do NOT modify the tests (they are the
    contract from Task 1). Modify the implementation instead.

    Commit: `feat(04-02): implement zoho writer (update/complete/delete/write-todoist-id)`
  </action>
  <verify>
    <automated>cd /data/home/zoho-todoist-sync &amp;&amp; python -m pytest tests/unit/test_zoho_writer.py -x -q &amp;&amp; python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/zoho/writer.py` exists
    - `grep -F "async def update_zoho_task" app/zoho/writer.py` returns 1
    - `grep -F "async def complete_zoho_task" app/zoho/writer.py` returns 1
    - `grep -F "async def delete_zoho_task" app/zoho/writer.py` returns 1
    - `grep -F "async def write_todoist_id_to_zoho" app/zoho/writer.py` returns 1
    - `grep -F "def _zoho_handle" app/zoho/writer.py` returns 1
    - `grep -F "async def _send_deletion_notification" app/zoho/writer.py` returns 1
    - `grep -F "todoist_to_zoho_priority" app/zoho/writer.py` returns 1+
    - `grep -F "zoho_terminal_statuses_list" app/zoho/writer.py` returns 1+ (NOT hardcoded "Completed")
    - `grep -F '"Completed"' app/zoho/writer.py` returns 0 (must not hardcode - EDGE-4)
    - `grep -F "zoho_field_cache.get" app/zoho/writer.py` returns 1+
    - `grep -E "status_code == 207" app/zoho/writer.py` returns 1 (Pitfall 2)
    - `grep -E "status_code == 404" app/zoho/writer.py` returns 1 (idempotent delete - Pitfall 5)
    - `grep -F "resend.Emails.send_async" app/zoho/writer.py` returns 1
    - `grep -F "manuelkuhs@gmail.com" app/zoho/writer.py` returns 1
    - `grep -F "ZOHO_EU_BASE_URL" app/zoho/writer.py` returns 2+ (imported + used at least once)
    - `python -c "import app.zoho.writer"` exits 0
    - pytest tests/unit/test_zoho_writer.py exits 0 (all 15 GREEN)
    - pytest tests/ exits 0 (no regression across full suite)
    - git log -1 --oneline contains "feat(04-02)"
  </acceptance_criteria>
  <done>All 15 writer tests GREEN; full suite GREEN; commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync worker -> Zoho API | Outbound HTTPS; token is a secret (rotated access token) |
| sync worker -> Resend API | Outbound HTTPS; token is a secret (RESEND_API_KEY) |
| Todoist content -> Zoho Subject field | Untrusted text persisted to Zoho; normalised upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | log.info/error in writer | mitigate | Log only IDs (zoho_id, todoist_id, status). NEVER log `access_token` value. Verified by absence of `access_token` field in log call sites (grep app/zoho/writer.py shows no `access_token=` in log calls). |
| T-04-08 | Information Disclosure | _send_deletion_notification email body | mitigate | Body contains only zoho_task_id literal - no tokens, no API keys, no field values. |
| T-04-09 | Tampering | Zoho PUT payload constructed from untrusted input (title/priority) | mitigate | `normalised.priority` is produced by priority mapping with fallback to "Low"; `normalised.title` is NFC+stripped by Phase 1 normaliser before reaching the writer. Writer trusts the NormalisedTask contract. |
| T-04-10 | Denial of Service (duplicate email flood) | delete_zoho_task on already-deleted record | mitigate | 404 short-circuit returns BEFORE `_send_deletion_notification` - test `test_delete_zoho_task_404_idempotent_no_email` proves `len(sent) == 0` when already deleted. |
| T-04-11 | Repudiation (silent failure) | EDGE-6 Resend exception swallowed | mitigate | `log.error("resend_email_failed", error=str(exc))` emits a structured log event for every swallowed failure. |
| T-04-12 | Spoofing (SSRF via zoho_task_id path) | Path construction f"{ZOHO_EU_BASE_URL}/Tasks/{zoho_task_id}" | accept | zoho_task_id originates from Zoho webhook or Zoho API responses (trusted source, Zoho-generated numeric ID). No user-controllable URL prefix. Risk is low and well-understood; path constant + numeric ID pattern bounds the attack surface. |
| T-04-13 | Tampering | write_todoist_id_to_zoho writes to field resolved at startup | mitigate | Guard clause raises ZohoAPIError if field_api_name is missing (Pitfall 3). Startup log already warns when resolution fails (Phase 2 main.py). Test `test_write_todoist_id_to_zoho_raises_when_field_not_resolved` enforces the guard. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_zoho_writer.py -x -q` - all 15 tests pass
2. `python -m pytest tests/ -x -q` - full suite passes (no regression)
3. `python -c "from app.zoho.writer import update_zoho_task, complete_zoho_task, delete_zoho_task, write_todoist_id_to_zoho"` exits 0
4. Hardcoded "Completed" is absent: `grep -F '"Completed"' app/zoho/writer.py` returns 0
</verification>

<success_criteria>
- Four Zoho write functions exported from `app/zoho/writer.py`, each covered by at least one test
- SYNC-3: PUT body uses {Subject, Due_Date, Priority} with reverse-mapped Priority strings
- SYNC-6: write_todoist_id_to_zoho uses zoho_field_cache['todoist_task_id_api_name'] dynamically
- EDGE-3: Due_Date=None serialises to JSON null (not omitted)
- EDGE-4: complete uses settings.zoho_terminal_statuses_list[0]; hardcoded "Completed" absent
- EDGE-2 + EDGE-6: delete sends Resend email; Resend failure logged (not raised)
- Pitfall 2: 207 Multi-Status inspected per-record; error status raises ZohoAPIError
- Pitfall 3: write_todoist_id_to_zoho raises before HTTP call when field unresolved
- Pitfall 5: 404 on delete returns early without email
- All four auth/404/429/other responses map to the correct typed exception
</success_criteria>

<output>
After completion, create `.planning/phases/04-write-operations/04-02-SUMMARY.md` documenting:
- Artifacts created (writer module, test file)
- Test count and GREEN status
- Any deviations from plan (e.g., 204 vs 200 on DELETE)
- Confirmation: zoho_field_cache guard works as designed
</output>
